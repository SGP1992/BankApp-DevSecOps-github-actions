name: deploy Banking App
on:
    workflow_dispatch:


jobs:
    deploy-BankApp:
        runs-on: ubuntu-latest
        steps:

            - name: checkout code.
              uses: actions/checkout@v4
